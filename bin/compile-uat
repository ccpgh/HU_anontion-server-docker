#!/bin/bash

PASSWORD1="`LC_ALL=C < /dev/urandom tr -dc 'A-Za-z0-9!@#$%^&*' | head -c $((12 + RANDOM % 13))`"

PASSWORD2="`LC_ALL=C < /dev/urandom tr -dc 'A-Za-z0-9!@#$%^&*' | head -c $((12 + RANDOM % 13))`"

. ./compile uat "${PASSWORD1}" "${PASSWORD2}"
